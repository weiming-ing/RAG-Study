# 🔄 Grundläggande agentarbetsflöden med Azure OpenAI (Responses API) (.NET)

## 📋 Handledning för arbetsflödesorkestrering

Denna anteckningsbok visar hur man bygger sofistikerade **agentarbetsflöden** med Microsoft Agent Framework för .NET och Azure OpenAI (Responses API). Du lär dig att skapa flerstegs affärsprocesser där AI-agenter samarbetar för att utföra komplexa uppgifter genom strukturerade orkestreringsmönster.

## 🎯 Läromål

### 🏗️ **Grunderna i arbetsflödesarkitektur**
- **Workflow Builder**: Designa och orkestrera komplexa flerstegs AI-processer
- **Agentkoordinering**: Koordinera flera specialiserade agenter inom arbetsflöden
- **Azure OpenAI (Responses API)**: Använd Azure OpenAI Responses API i arbetsflöden
- **Visuell arbetsflödesdesign**: Skapa och visualisera arbetsflödesstrukturer för bättre förståelse

### 🔄 **Mönster för processorkestrering**
- **Sekventiell bearbetning**: Kjedja flera agentuppgifter i logisk ordning
- **Statushantering**: Behåll kontext och dataflöde genom arbetsflödets steg
- **Felfunktioner**: Implementera robust felåterställning och arbetsflödesresiliens
- **Prestandaoptimering**: Designa effektiva arbetsflöden för företagsstorskaliga operationer

### 🏢 **Företagsanvändningsområden för arbetsflöden**
- **Automatisering av affärsprocesser**: Automatisera komplexa organisationsarbetsflöden
- **Innehållsproduktionspipeline**: Redaktionella arbetsflöden med gransknings- och godkännandesteg
- **Automatisering av kundservice**: Flerstegs kundförfrågningshantering
- **Datahanteringsarbetsflöden**: ETL-arbetsflöden med AI-driven transformation

## ⚙️ Förutsättningar & installation

### 📦 **Nödvändiga NuGet-paket**

Denna arbetsflödesdemonstration använder flera viktiga .NET-paket:

```xml
<!-- Core AI Framework -->
<PackageReference Include="Microsoft.Extensions.AI" Version="10.*" />

<!-- Azure OpenAI (Responses API) -->
<PackageReference Include="Azure.AI.OpenAI" Version="2.1.0" />
<PackageReference Include="Azure.Identity" Version="1.15.0" />

<!-- Agent Framework (Local Development) -->
<!-- Microsoft.Agents.AI.dll - Core agent abstractions -->
<!-- Microsoft.Agents.AI.OpenAI.dll - Azure OpenAI (Responses API) integration -->

<!-- Configuration and Environment -->
<PackageReference Include="DotNetEnv" Version="3.1.1" />
```

### 🔑 **Azure OpenAI-konfiguration**

**Miljöinställning (.env-fil):**
```env
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-5-mini
```

**Azure OpenAI-åtkomst:**
1. Skapa en Azure OpenAI-resurs i Azure-portalen
2. Distribuera en modell (t.ex. `gpt-5-mini`) och notera distributionsnamnet
3. Logga in med `az login` och konfigurera miljövariabler som visas ovan

### 🏗️ **Översikt över arbetsflödesarkitektur**

```mermaid
graph TD
    A[Arbetsflödesbyggare] --> B[Agentregister]
    B --> C[Arbetsflödeskörningsmotor]
    C --> D[Agent 1: Innehållsgenerator]
    C --> E[Agent 2: Innehållsgranskare] 
    D --> F[Arbetsflödesresultat]
    E --> F
    G[Azure OpenAI (svars-API)] --> D
    G --> E
```

**Nyckelkomponenter:**
- **WorkflowBuilder**: Huvudorkestreringsmotorn för att designa arbetsflöden
- **AIAgent**: Individuella specialiserade agenter med specifika förmågor
- **Azure OpenAI-klient**: Integration med Azure OpenAI Responses API
- **Exekveringskontext**: Hanterar status och dataflöde mellan arbetsflödets steg

## 🎨 **Företagsmönster för arbetsflödesdesign**

### 📝 **Innehållsproduktionsarbetsflöde**
```
User Request → Content Generation → Quality Review → Final Output
```

### 🔍 **Dokumenthanteringspipeline**
```
Document Input → Analysis → Extraction → Validation → Structured Output
```

### 💼 **Business Intelligence-arbetsflöde**
```
Data Collection → Processing → Analysis → Report Generation → Distribution
```

### 🤝 **Automatisering av kundservice**
```
Customer Inquiry → Classification → Processing → Response Generation → Follow-up
```

## 🏢 **Företagsfördelar**

### 🎯 **Tillförlitlighet & Skalbarhet**
- **Deterministisk exekvering**: Konsekventa, upprepbara arbetsflödesresultat
- **Felåterställning**: Smidig hantering av fel i alla arbetsflödets steg
- **Prestandaövervakning**: Spåra exekveringsmått och optimeringsmöjligheter
- **Resurshantering**: Effektiv allokering och användning av AI-modellresurser

### 🔒 **Säkerhet & Efterlevnad**
- **Säker autentisering**: Microsoft Entra ID-autentisering via `az login` (AzureCliCredential)
- **Revisionsloggar**: Komplett loggning av arbetsflödesejsekvering och beslutspunkter
- **Åtkomstkontroll**: Granulära behörigheter för exekvering och övervakning av arbetsflöden
- **Dataskydd**: Säker hantering av känslig information genom hela arbetsflöden

### 📊 **Observerbarhet & Hantering**
- **Visuell arbetsflödesdesign**: Tydlig representation av processflöden och beroenden
- **Exekveringsövervakning**: Realtidsspårning av arbetsflödets framsteg och prestanda
- **Felrapportering**: Detaljerad felsöknings- och analyskapacitet
- **Prestandaanalys**: Mätvärden för optimering och kapacitetsplanering

Låt oss bygga ditt första företagsklara AI-arbetsflöde! 🚀

## 💻 Köra koden

Den kompletta implementationen finns i `01.dotnet-agent-framework-workflow-ghmodel-basic.cs`. Denna fil demonstrerar:

1. **Miljökonfiguration** - Laddar Azure OpenAI-konfiguration från `.env`-fil
2. **Azure OpenAI-klientuppsättning** - Konfigurerar klienten för att använda Azure OpenAI Responses API
3. **Agentuppbyggnad** - Definierar specialiserade agenter (Front Desk och Concierge)
4. **Workflow Builder** - Skapar ett flergents arbetsflöde med sekventiell bearbetning
5. **Arbetsflödeskörning** - Kör arbetsflödet med strömningsresultat

### 🚀 Köra exemplet

```bash
# Gör skriptet körbart (Unix/Linux/macOS)
chmod +x 01.dotnet-agent-framework-workflow-ghmodel-basic.cs

# Kör arbetsflödet
./01.dotnet-agent-framework-workflow-ghmodel-basic.cs
```

Eller på Windows:
```powershell
dotnet run 01.dotnet-agent-framework-workflow-ghmodel-basic.cs
```

### 📝 Förväntad utdata

Arbetsflödet kommer att:
1. Acceptera din reseförfrågan ("Jag skulle vilja åka till Paris")
2. Front Desk-agenten ger en initial rekommendation
3. Concierge-agenten granskar och förfinar rekommendationen
4. Slutlig utdata visar hela konversationsströmmen

### 🔧 Anpassning

Du kan anpassa arbetsflödet genom att:
- Ändra agenters instruktioner för att förändra deras beteende
- Lägga till fler agenter för att skapa komplexa flerstegsarbetsflöden
- Ändra användarmeddelandet för att testa olika scenarier
- Justera arbetsflödets kanter för att skapa olika exekveringsmönster

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Ansvarsfriskrivning**:
Detta dokument har översatts med hjälp av AI-översättningstjänsten [Co-op Translator](https://github.com/Azure/co-op-translator). Även om vi strävar efter noggrannhet, var vänlig notera att automatiska översättningar kan innehålla fel eller brister. Det ursprungliga dokumentet på dess modersmål bör betraktas som den auktoritativa källan. För kritisk information rekommenderas professionell mänsklig översättning. Vi ansvarar inte för några missförstånd eller feltolkningar som uppstår till följd av användningen av denna översättning.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->