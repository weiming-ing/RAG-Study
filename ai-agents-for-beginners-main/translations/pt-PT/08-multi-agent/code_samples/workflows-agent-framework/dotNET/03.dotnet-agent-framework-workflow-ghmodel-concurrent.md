# ⚡ Fluxos de Trabalho Concorrentes com Agentes usando Azure OpenAI (API de Respostas) (.NET)

## 📋 Tutorial de Processamento Paralelo de Alto Desempenho

Este notebook demonstra **padrões de fluxos de trabalho concorrentes** usando o Microsoft Agent Framework para .NET e Azure OpenAI (API de Respostas). Vai aprender a construir fluxos de trabalho de processamento paralelo e alto desempenho que maximizam o rendimento ao executar múltiplos agentes de IA simultaneamente, mantendo a coordenação e a consistência dos dados.

## 🎯 Objetivos de Aprendizagem

### 🚀 **Fundamentos do Processamento Concorrente**
- **Execução Paralela de Agentes**: Execute múltiplos agentes de IA simultaneamente para desempenho máximo
- **Padrões Async/Await**: Aproveite o modelo de programação async do .NET para concorrência eficiente
- **Azure OpenAI (API de Respostas)**: Coordene múltiplas chamadas concorrentes à API de Respostas do Azure OpenAI
- **Gestão de Recursos**: Gerencie eficientemente os recursos dos modelos de IA em operações concorrentes

### 🏗️ **Arquitetura Avançada de Concorrência**
- **Paralelismo Baseado em Tarefas**: Use a Task Parallel Library do .NET para execução concorrente ótima
- **Padrões de Sincronização**: Coordene agentes concorrentes evitando condições de corrida
- **Balanceamento de Carga**: Distribua o trabalho eficientemente pela capacidade concorrente disponível
- **Tolerância a Falhas**: Trate falhas individuais de agentes sem parar todo o fluxo de trabalho

### 🏢 **Aplicações Concorrentes Empresariais**
- **Processamento de Documentos em Alto Volume**: Processe múltiplos documentos simultaneamente
- **Análise de Conteúdo em Tempo Real**: Análise concorrente de fluxos de dados recebidos
- **Otimização de Processamento em Lote**: Maximize o rendimento para operações em larga escala de processamento de dados
- **Análise Multi-Modal**: Processamento paralelo de diferentes tipos e formatos de conteúdo

## ⚙️ Pré-Requisitos & Configuração

### 📦 **Pacotes NuGet Requeridos**

Pacotes essenciais para fluxos de trabalho concorrentes de alto desempenho:

```xml
<!-- Core AI Framework with Async Support -->
<PackageReference Include="Microsoft.Extensions.AI" Version="9.9.0" />

<!-- Azure OpenAI (Responses API) -->
<PackageReference Include="Azure.AI.OpenAI" Version="2.1.0" />

<!-- Azure Identity and Async LINQ for Advanced Operations -->
<PackageReference Include="Azure.Identity" Version="1.15.0" />
<PackageReference Include="System.Linq.Async" Version="6.0.3" />

<!-- Local Agent Framework References -->
<!-- Microsoft.Agents.AI.dll - Core agent abstractions with async support -->
<!-- Microsoft.Agents.AI.OpenAI.dll - Azure OpenAI (Responses API) integration with concurrency -->
```

### 🔑 **Configuração do Azure OpenAI**

**Configuração do Ambiente (ficheiro .env):**
```env
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-5-mini
```

**Considerações de Processamento Concorrente:**
```csharp
// Configure for concurrent operations
var clientOptions = new AzureOpenAIClientOptions()
{
    // Configure network timeout for concurrent requests
    NetworkTimeout = TimeSpan.FromMinutes(5)
};
```

### 🏗️ **Arquitetura do Fluxo de Trabalho Concorrente**

```mermaid
graph TD
    A[Entrada do Fluxo de Trabalho] --> B[Distribuição de Tarefas]
    B --> C[Pool de Agentes Concorrentes]
    C --> D[Tarefa do Agente 1]
    C --> E[Tarefa do Agente 2]
    C --> F[Tarefa do Agente 3]
    C --> G[Tarefa do Agente N]
    
    D --> H[Agregação de Resultados]
    E --> H
    F --> H
    G --> H
    
    H --> I[Saída Sincronizada]
    
    J[Azure OpenAI (API de Respostas)] --> D
    J --> E
    J --> F
    J --> G
    
    K[Agendador de Tarefas .NET] --> C
```

**Componentes Principais:**
- **Task Parallel Library**: Suporte incorporado do .NET para operações concorrentes
- **Pool de Agentes**: Múltiplas instâncias de agentes para processamento paralelo
- **Agregação de Resultados**: Coordenação e fusão dos resultados dos agentes concorrentes
- **Pontos de Sincronização**: Garantir a consistência dos dados nas operações concorrentes

## 🎨 **Padrões de Design para Fluxos de Trabalho Concorrentes**

### 🔍 **Pesquisa & Análise Paralelas**
```
Research Topic → Concurrent Research Agents → Result Synthesis → Final Report
```

### 📊 **Processamento de Dados Multi-Fonte**
```
Data Sources → Parallel Processing Agents → Data Integration → Unified Output
```

### 🎭 **Pipeline de Geração de Conteúdo**
```
Content Requirements → Concurrent Content Generators → Quality Review → Final Content
```

### 🔄 **Processamento Fan-Out/Fan-In**
```
Single Input → Multiple Concurrent Processors → Result Aggregation → Single Output
```

## 🏢 **Benefícios de Desempenho Empresarial**

### ⚡ **Rendimento & Escalabilidade**
- **Escalonamento Linear de Desempenho**: Adicione mais agentes concorrentes para aumentar o rendimento
- **Utilização de Recursos**: Eficiência máxima da capacidade disponível do modelo de IA
- **Redução do Tempo de Processamento**: Redução significativa do tempo através de execução paralela
- **Escalonamento Elástico**: Ajuste dinâmico do número de agentes concorrentes com base no volume de trabalho

### 🛡️ **Confiabilidade & Resiliência**
- **Isolamento de Falhas**: Falhas de agentes individuais não afetam outras operações concorrentes
- **Degradação Elegante**: O sistema continua operando com capacidade reduzida de agentes
- **Recuperação de Erros**: Mecanismos automáticos de retentativa para operações concorrentes falhadas
- **Distribuição de Carga**: Distribuição equilibrada do trabalho entre agentes disponíveis

### 📊 **Monitorização de Desempenho**
- **Métricas de Execução Concorrente**: Acompanhamento do desempenho de todas as operações paralelas
- **Análise de Uso de Recursos**: Monitorização do uso de CPU, memória e rede
- **Análise de Rendimento**: Medição dos ganhos de eficiência do processamento concorrente
- **Deteção de Gargalos**: Identificação e resolução de restrições de desempenho

### 🔧 **Desenvolvimento & Operações**
- **Modelo de Programação Async**: Aproveite os maduros padrões async/await do .NET
- **Coordenação de Tarefas**: Capacidades incorporadas de gestão e coordenação de tarefas
- **Tratamento de Exceções**: Tratamento completo de erros para operações concorrentes
- **Suporte a Debugging**: Ferramentas de debug do Visual Studio para fluxos de trabalho concorrentes

Vamos construir fluxos de trabalho concorrentes de IA de alto desempenho com .NET! 🚀

## 💻 Executar o Código

A implementação completa está disponível em `03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs`. Este ficheiro demonstra um **fluxo de trabalho concorrente Fan-Out/Fan-In** para planeamento de viagens:

### 🏗️ **Arquitetura do Fluxo de Trabalho**

```
User Request → ConcurrentStartExecutor → [Researcher Agent || Planner Agent] → ConcurrentAggregationExecutor → Final Output
```

**Componentes Principais:**

1. **ConcurrentStartExecutor**: Difunde o pedido do utilizador para todos os agentes simultaneamente
2. **Agent Researcher**: Analisa destinos e atrações concorrentemente
3. **Agent Planner**: Cria planos de viagem detalhados concorrentemente
4. **ConcurrentAggregationExecutor**: Recolhe e funde resultados de ambos os agentes

### 🎯 **Padrão Fan-Out/Fan-In**

Este fluxo de trabalho demonstra o padrão clássico **Fan-Out/Fan-In**:
- **Fan-Out**: Uma mensagem de entrada é difundida para múltiplos agentes simultaneamente
- **Processamento Concorrente**: Múltiplos agentes trabalham em paralelo na mesma tarefa
- **Fan-In**: Resultados de todos os agentes são recolhidos e agregados numa única saída

### 🚀 Executar o Exemplo

```bash
# Torne o script executável (Unix/Linux/macOS)
chmod +x 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs

# Execute o fluxo de trabalho concorrente
./03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

Ou no Windows:
```powershell
dotnet run 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

### 📝 Saída Esperada

O fluxo de trabalho irá:
1. **Transmitir Pedido**: Enviar "Planeia uma viagem para Seattle em Dezembro" para ambos os agentes
2. **Processamento Concorrente**: Ambos os agentes trabalham simultaneamente:
   - O pesquisador identifica atrações e detalhes
   - O planeador cria o itinerário e logística
3. **Agregação**: Combinar ambas as respostas numa saída compreensiva
4. **Exibir Resultados**: Mostrar o plano de viagem fundido com toda a informação

### 🔧 Opções de Personalização

**Adicionar Mais Agentes Concorrentes:**
```csharp
// Create additional specialized agents
AIAgent budgetAgent = azureClient.GetOpenAIResponseClient(deployment).CreateAIAgent(
    name: "Budget-Agent", instructions: "Calculate travel costs...");

// Add to fan-out
var workflow = new WorkflowBuilder(startExecutor)
    .AddFanOutEdge(startExecutor, targets: [researcherAgent, plannerAgent, budgetAgent])
    .AddFanInEdge(aggregationExecutor, sources: [researcherAgent, plannerAgent, budgetAgent])
    .WithOutputFrom(aggregationExecutor)
    .Build();

// Update aggregation count
if (this._messages.Count == 3) { ... }
```

**Modificar Instruções do Agente:**
```csharp
const string ResearcherAgentInstructions = "Your custom instructions for research...";
const string PlanAgentInstructions = "Your custom instructions for planning...";
```

**Alterar a Tarefa:**
```csharp
StreamingRun run = await InProcessExecution.StreamAsync(
    workflow, 
    "Plan a European vacation for 2 weeks in summer"
);
```

### 🎯 Aplicações no Mundo Real

Este padrão concorrente é ideal para:
- **Criação de Conteúdo**: Múltiplos escritores criando secções diferentes simultaneamente
- **Revisão de Código**: Múltiplos revisores analisando código de diferentes perspetivas
- **Pesquisa de Mercado**: Análise paralela de diferentes segmentos de mercado
- **Processamento de Documentos**: Extração, análise e validação concorrentes
- **Análise Multi-Perspetiva**: Obtenção de pontos de vista diversos sobre a mesma entrada

### 🔍 Compreender os Executors Personalizados

**ConcurrentStartExecutor:**
- Implementa `IMessageHandler<string>` para aceitar input do tipo string
- Difunde mensagens para todos os agentes conectados
- Envia `TurnToken` para desencadear processamento concorrente

**ConcurrentAggregationExecutor:**
- Implementa `IMessageHandler<ChatMessage>` para receber respostas dos agentes
- Recolhe mensagens de forma thread-safe
- Agrega quando todas as respostas esperadas chegam
- Produz a saída final usando `context.YieldOutputAsync()`

### ⚡ Benefícios de Desempenho

**Concorrente vs Sequencial:**
- Sequencial: Agente1 (30s) → Agente2 (30s) = **60 segundos no total**
- Concorrente: Agente1 (30s) || Agente2 (30s) = **30 segundos no total**

**Melhoria do rendimento**: Até N× mais rápido para N agentes concorrentes (dependendo da carga e recursos)

### 🛡️ Tratamento de Erros

O fluxo de trabalho trata falhas de agentes individuais de forma elegante:
- Se um agente falhar, os outros continuam a processar
- O agregador pode implementar lógica de timeout
- Resultados parciais podem ser devolvidos se necessário

### 📊 Funcionalidades Avançadas

**Número Dinâmico de Agentes:**
Modifique a lógica de agregação para suportar contagens variáveis de agentes:

```csharp
private int _expectedAgentCount;
private readonly List<ChatMessage> _messages = [];

public async ValueTask HandleAsync(ChatMessage message, IWorkflowContext context)
{
    this._messages.Add(message);
    if (this._messages.Count == _expectedAgentCount)
    {
        // Process aggregation
    }
}
```

Este padrão concorrente é essencial para construir sistemas de agentes de IA escaláveis e de alto desempenho!

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Aviso Legal**:
Este documento foi traduzido utilizando o serviço de tradução automática [Co-op Translator](https://github.com/Azure/co-op-translator). Embora nos esforcemos pela precisão, esteja ciente de que traduções automáticas podem conter erros ou imprecisões. O documento original na sua língua nativa deve ser considerado a fonte autorizada. Para informações críticas, recomenda-se tradução profissional humana. Não nos responsabilizamos por quaisquer mal-entendidos ou interpretações incorretas resultantes da utilização desta tradução.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->