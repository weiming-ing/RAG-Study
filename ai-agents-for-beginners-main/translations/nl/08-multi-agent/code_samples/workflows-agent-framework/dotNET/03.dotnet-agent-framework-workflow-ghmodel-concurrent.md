# ⚡ Gelijktijdige Agent Workflows met Azure OpenAI (Responses API) (.NET)

## 📋 Tutorial voor Hoogwaardige Parallelle Verwerking

Deze notebook demonstreert **gelijktijdige workflowpatronen** met het Microsoft Agent Framework voor .NET en Azure OpenAI (Responses API). Je leert hoe je workflows voor parallelle verwerking met hoge prestaties bouwt die de doorvoer maximaliseren door meerdere AI-agents gelijktijdig uit te voeren, terwijl je coördinatie en dataconsistentie behoudt.

## 🎯 Leerdoelen

### 🚀 **Grondbeginselen van Gelijktijdige Verwerking**
- **Parallelle Agentuitvoering**: Voer meerdere AI-agents gelijktijdig uit voor maximale prestaties
- **Async/Await Patronen**: Maak gebruik van .NET's async programmeermodel voor efficiënte gelijktijdigheid
- **Azure OpenAI (Responses API)**: Coördineer meerdere gelijktijdige oproepen naar de Azure OpenAI Responses API
- **Resourcebeheer**: Beheer AI-modelbronnen efficiënt over gelijktijdige bewerkingen

### 🏗️ **Geavanceerde Concurrency Architectuur**
- **Taakgebaseerde Parallelisatie**: Gebruik de .NET Task Parallel Library voor optimale gelijktijdige uitvoering
- **Synchronisatiepatronen**: Coördineer gelijktijdige agents en voorkom racecondities
- **Load Balancing**: Verdeel werk efficiënt over beschikbare concurrerende verwerkingscapaciteit
- **Fouttolerantie**: Verwerk individuele agentfouten zonder dat de hele workflow stopt

### 🏢 **Enterprise Gelijktijdige Toepassingen**
- **Documentverwerking met Hoog Volume**: Verwerk meerdere documenten gelijktijdig
- **Realtime Inhoudsanalyse**: Gelijktijdige analyse van binnenkomende datastromen
- **Optimalisatie van Batchverwerking**: Maximaliseer doorvoer voor grootschalige gegevensverwerking
- **Multi-Modal Analyse**: Parallelle verwerking van verschillende soorten en formaten inhoud

## ⚙️ Vereisten & Setup

### 📦 **Benodigde NuGet Packages**

Essentiële packages voor hoogstaande gelijktijdige workflows:

```xml
<!-- Core AI Framework with Async Support -->
<PackageReference Include="Microsoft.Extensions.AI" Version="9.9.0" />

<!-- Azure OpenAI (Responses API) -->
<PackageReference Include="Azure.AI.OpenAI" Version="2.1.0" />

<!-- Azure Identity and Async LINQ for Advanced Operations -->
<PackageReference Include="Azure.Identity" Version="1.15.0" />
<PackageReference Include="System.Linq.Async" Version="6.0.3" />

<!-- Local Agent Framework References -->
<!-- Microsoft.Agents.AI.dll - Core agent abstractions with async support -->
<!-- Microsoft.Agents.AI.OpenAI.dll - Azure OpenAI (Responses API) integration with concurrency -->
```

### 🔑 **Azure OpenAI Configuratie**

**Omgevingsinstellingen (.env bestand):**
```env
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-5-mini
```

**Overwegingen bij Gelijktijdige Verwerking:**
```csharp
// Configure for concurrent operations
var clientOptions = new AzureOpenAIClientOptions()
{
    // Configure network timeout for concurrent requests
    NetworkTimeout = TimeSpan.FromMinutes(5)
};
```

### 🏗️ **Concurrent Workflow Architectuur**

```mermaid
graph TD
    A[Workflow Invoer] --> B[Taakverdeling]
    B --> C[Gelijktijdige Agent Pool]
    C --> D[Agent Taak 1]
    C --> E[Agent Taak 2]
    C --> F[Agent Taak 3]
    C --> G[Agent Taak N]
    
    D --> H[Resultaataggregatie]
    E --> H
    F --> H
    G --> H
    
    H --> I[Gesynchroniseerde Uitvoer]
    
    J[Azure OpenAI (Responses API)] --> D
    J --> E
    J --> F
    J --> G
    
    K[.NET Taakplanner] --> C
```

**Belangrijke Componenten:**
- **Task Parallel Library**: ingebouwde ondersteuning van .NET voor gelijktijdige bewerkingen
- **Agent Pool**: Meerdere agent instanties voor parallelle verwerking
- **Resultaataggregatie**: Coördinatie en samenvoeging van gelijktijdige agentresultaten
- **Synchronisatiepunten**: Zorg voor dataconsistentie over gelijktijdige bewerkingen

## 🎨 **Concurrent Workflow Ontwerp Patronen**

### 🔍 **Parallel Onderzoek & Analyse**
```
Research Topic → Concurrent Research Agents → Result Synthesis → Final Report
```

### 📊 **Multi-Bron Data Verwerking**
```
Data Sources → Parallel Processing Agents → Data Integration → Unified Output
```

### 🎭 **Content Generatie Pipeline**
```
Content Requirements → Concurrent Content Generators → Quality Review → Final Content
```

### 🔄 **Fan-Out/Fan-In Verwerking**
```
Single Input → Multiple Concurrent Processors → Result Aggregation → Single Output
```

## 🏢 **Enterprise Prestatievoordelen**

### ⚡ **Doorvoersnelheid & Schaalbaarheid**
- **Lineaire Prestatie Schaling**: Voeg meer gelijktijdige agents toe om doorvoer te verhogen
- **Resourcegebruik**: Maximale efficiëntie van beschikbare AI-modelcapaciteit
- **Verminderde Verwerkingstijd**: Significante tijdbesparing door parallelle uitvoering
- **Elastische Schaling**: Pas het aantal gelijktijdige agents dynamisch aan op de werklast

### 🛡️ **Betrouwbaarheid & Veerkracht**
- **Foutisolatie**: Individuele agentfouten beïnvloeden andere gelijktijdige bewerkingen niet
- **Gracieuze Degradatie**: Systeem blijft werken met verminderde agentcapaciteit
- **Foutherstel**: Automatische herhaalmechanismen voor mislukte gelijktijdige bewerkingen
- **Werkverdeling**: Gelijke verdeling van werk over beschikbare agents

### 📊 **Prestatiemonitoring**
- **Metingen Gelijktijdige Uitvoering**: Volg prestaties van alle parallelle bewerkingen
- **Analyse Resourcegebruik**: Monitor CPU-, geheugen- en netwerkgebruik
- **Doorvoer Analyse**: Meet efficiëntiewinst van gelijktijdige verwerking
- **Bottleneck Detectie**: Identificeer en los prestatiebeperkingen op

### 🔧 **Ontwikkeling & Operaties**
- **Async Programmeermodel**: Maak gebruik van .NET's volwassen async/await patronen
- **Taakcoördinatie**: Ingebouwde taakbeheer- en coördinatiefunctionaliteit
- **Foutafhandeling**: Uitgebreide foutafhandeling voor gelijktijdige bewerkingen
- **Debugondersteuning**: Visual Studio debugging tools voor gelijktijdige workflows

Laten we hoogstaande gelijktijdige AI-workflows bouwen met .NET! 🚀

## 💻 De Code Uitvoeren

De volledige implementatie is beschikbaar in `03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs`. Dit bestand demonstreert een **Fan-Out/Fan-In gelijktijdige workflow** voor reisplanning:

### 🏗️ **Workflow Architectuur**

```
User Request → ConcurrentStartExecutor → [Researcher Agent || Planner Agent] → ConcurrentAggregationExecutor → Final Output
```

**Belangrijke Componenten:**

1. **ConcurrentStartExecutor**: Stuurt de gebruikersaanvraag gelijktijdig naar alle agents
2. **Researcher Agent**: Analyseert bestemmingen en attracties gelijktijdig
3. **Planner Agent**: Maakt gedetailleerde reisplannen gelijktijdig
4. **ConcurrentAggregationExecutor**: Verzamelt en combineert resultaten van beide agents

### 🎯 **Fan-Out/Fan-In Patroon**

Deze workflow demonstreert het klassieke **Fan-Out/Fan-In** patroon:
- **Fan-Out**: Eén invoerbericht wordt gelijktijdig naar meerdere agents uitgezonden
- **Gelijktijdige Verwerking**: Meerdere agents werken parallel aan dezelfde taak
- **Fan-In**: Resultaten van alle agents worden verzameld en samengevoegd tot één output

### 🚀 Het Voorbeeld Uitvoeren

```bash
# Maak het script uitvoerbaar (Unix/Linux/macOS)
chmod +x 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs

# Voer de gelijktijdige workflow uit
./03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

Of op Windows:
```powershell
dotnet run 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

### 📝 Verwachte Output

De workflow zal:
1. **Verzoek Verzenden**: "Plan een reis naar Seattle in december" naar beide agents sturen
2. **Gelijktijdige Verwerking**: Beide agents werken tegelijkertijd:
   - Researcher identificeert attracties en details
   - Planner maakt het reisplan en de logistiek
3. **Aggregatie**: Combineer beide antwoorden tot een uitgebreid resultaat
4. **Resultaten Weergeven**: Toon het samengevoegde reisplan met alle informatie

### 🔧 Aanpassingsopties

**Voeg Meer Gelijktijdige Agents toe:**
```csharp
// Create additional specialized agents
AIAgent budgetAgent = azureClient.GetOpenAIResponseClient(deployment).CreateAIAgent(
    name: "Budget-Agent", instructions: "Calculate travel costs...");

// Add to fan-out
var workflow = new WorkflowBuilder(startExecutor)
    .AddFanOutEdge(startExecutor, targets: [researcherAgent, plannerAgent, budgetAgent])
    .AddFanInEdge(aggregationExecutor, sources: [researcherAgent, plannerAgent, budgetAgent])
    .WithOutputFrom(aggregationExecutor)
    .Build();

// Update aggregation count
if (this._messages.Count == 3) { ... }
```

**Pas Agentinstructies aan:**
```csharp
const string ResearcherAgentInstructions = "Your custom instructions for research...";
const string PlanAgentInstructions = "Your custom instructions for planning...";
```

**Verander de Taak:**
```csharp
StreamingRun run = await InProcessExecution.StreamAsync(
    workflow, 
    "Plan a European vacation for 2 weeks in summer"
);
```

### 🎯 Praktische Toepassingen

Dit gelijktijdig patroon is ideaal voor:
- **Content Creatie**: Meerdere schrijvers creëren gelijktijdig verschillende secties
- **Code Review**: Meerdere reviewers analyseren code vanuit verschillende invalshoeken
- **Marktonderzoek**: Parallelle analyse van verschillende marktsegmenten
- **Documentverwerking**: Gelijktijdige extractie, analyse en validatie
- **Multi-Perspectief Analyse**: Verschillende gezichtspunten op dezelfde input verkrijgen

### 🔍 Begrip van Aangepaste Executors

**ConcurrentStartExecutor:**
- Implementeert `IMessageHandler<string>` om stringinvoer te accepteren
- Zendt berichten tegelijkertijd naar alle verbonden agents
- Stuurt `TurnToken` om gelijktijdige verwerking te activeren

**ConcurrentAggregationExecutor:**
- Implementeert `IMessageHandler<ChatMessage>` om agentantwoorden te ontvangen
- Verzamelt berichten op een thread-veilige manier
- Voert aggregatie uit als alle verwachte antwoorden binnen zijn
- Geeft de finale output terug via `context.YieldOutputAsync()`

### ⚡ Prestatievoordelen

**Gelijktijdig versus Sequentieel:**
- Sequentieel: Agent1 (30s) → Agent2 (30s) = **60 seconden totaal**
- Gelijktijdig: Agent1 (30s) || Agent2 (30s) = **30 seconden totaal**

**Doorvoerverbetering**: Tot N× sneller voor N gelijktijdige agents (afhankelijk van werklast en middelen)

### 🛡️ Foutafhandeling

De workflow verwerkt individuele agentfouten op een fraaie manier:
- Als een agent faalt, blijven de anderen doorgaan
- Aggregator kan time-outlogica toepassen
- Indien nodig kunnen gedeeltelijke resultaten worden geretourneerd

### 📊 Geavanceerde Functionaliteiten

**Dynamisch Aantal Agents:**
Pas de aggregatielogica aan om variabel aantal agents te ondersteunen:

```csharp
private int _expectedAgentCount;
private readonly List<ChatMessage> _messages = [];

public async ValueTask HandleAsync(ChatMessage message, IWorkflowContext context)
{
    this._messages.Add(message);
    if (this._messages.Count == _expectedAgentCount)
    {
        // Process aggregation
    }
}
```

Dit gelijktijdige workflowpatroon is essentieel voor het bouwen van hoogstaande, schaalbare AI-agentensystemen!

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Disclaimer**:
Dit document is vertaald met behulp van de AI vertaaldienst [Co-op Translator](https://github.com/Azure/co-op-translator). Hoewel we streven naar nauwkeurigheid, dient u er rekening mee te houden dat geautomatiseerde vertalingen fouten of onnauwkeurigheden kunnen bevatten. Het originele document in de oorspronkelijke taal moet worden beschouwd als de gezaghebbende bron. Voor kritieke informatie wordt professionele menselijke vertaling aanbevolen. Wij zijn niet aansprakelijk voor eventuele misverstanden of verkeerde interpretaties die voortvloeien uit het gebruik van deze vertaling.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->