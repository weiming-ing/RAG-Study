# 🔀 זרימות עבודה מותנות לסוכנים עם Microsoft Foundry (.NET)

## 📋 מדריך זרימות עבודה מבוססות החלטות חכמות

מחברת זו מדגימה **תבניות זרימות עבודה מותנות** באמצעות Microsoft Foundry ו-Microsoft Agent Framework עבור .NET. תלמד כיצד לבנות זרימות עבודה מורכבות המונעות מהחלטות החכמות המסווגות עיבוד בצורה חכמה על בסיס ניתוח AI, כללי עסק ותנאים דינמיים לאוטומציה ברמה ארגונית.

## 🎯 מטרות הלמידה

### 🧠 **ארכיטקטורה חכמה להחלטות**
- **יישום לוגיקה מותנת**: בניית עצי החלטה מורכבים עם נקודות הסתעפות מרובות
- **ניתוב מונחה AI**: שימוש במודלים של Microsoft Foundry לקבלת החלטות ניתוב חכמות
- **התאמת זרימות עבודה דינמית**: שינוי התנהגות הזרימה על בסיס ניתוח ותנאים בזמן ריצה
- **שילוב כללי ארגון**: שילוב הלוגיקה העסקית ודרישות תאימות בתוך זרימות העבודה

### 🔀 **תבניות מותנות מורכבות**
- **קבלת החלטות מרובת קריטריונים**: הערכת גורמים רבים לקבלת החלטות ניתוב
- **עיבוד מבוסס הקשר**: קבלת החלטות על פי הקשר והיסטוריית זרימת העבודה
- **התאמת זרימה אדפטיבית**: התאמת מסלולי עיבוד בזמן אמת בהתאם לתנאים משתנים
- **שילוב מנועי חוקים**: יישום מנועי חוקים עסקיים מתקדם בתוך זרימות העבודה

### 🏢 **יישומים מותנים ארגוניים**
- **סיווג וניתוב מסמכים**: סיווג וניתוב אוטומטיים של מסמכים לזרימות עבודה מתאימות
- **מיון שירות לקוחות**: ניתוב חכם של פניות לקבוצות טיפול ייעודיות
- **עיבוד תאימות וסיכונים**: יישום תהליכי אימות וביקורת שונים על בסיס הערכת סיכון
- **זרימות עבודה לאבטחת איכות**: ניתוב תוכן לתהליכי ביקורת תוך התחשבות במדדי איכות

## ⚙️ דרישות מוקדמות והגדרה

### 📦 **חבילות NuGet נדרשות**

חבילות מתקדמות לעיבוד זרימות עבודה מותנות:

```xml
<!-- Core AI Framework -->
<PackageReference Include="Microsoft.Extensions.AI" Version="9.9.0" />

<!-- Azure AI Agents with Persistent State -->
<PackageReference Include="Azure.AI.Agents.Persistent" Version="1.2.0-beta.5" />

<!-- Azure Identity and Utilities -->
<PackageReference Include="Azure.Identity" Version="1.15.0" />
<PackageReference Include="System.Linq.Async" Version="6.0.3" />
<PackageReference Include="DotNetEnv" Version="3.1.1" />

<!-- Local Workflow Framework References -->
<!-- Microsoft.Agents.Workflows.dll - Advanced workflow orchestration -->
<!-- Microsoft.Agents.AI.AzureAI.dll - Microsoft Foundry integration -->
<!-- Microsoft.Agents.AI.dll - Core agent abstractions -->
```

### 🔑 **הגדרת Microsoft Foundry**

**משאבי Azure נדרשים:**
- סביבת עבודה של Microsoft Foundry עם מודלים לעיבוד מותנה
- מנוי Azure עם מכסים והרשאות מתאימות לחישוב
- מודלים של AI מופעלים להחלטות וניתוח תוכן
- (אופציונלי) חיבור ל-Bing Search API עבור יכולות עיגון

**הגדרת סביבה (קובץ .env):**
```env
# Microsoft Foundry Configuration
AZURE_AI_PROJECT_ENDPOINT=https://your-project.cognitiveservices.azure.com/
BING_CONNECTION_ID=your-bing-connection-id
```

**הגדרת אימות:**
```csharp
// Azure CLI or Managed Identity authentication
using Azure.Identity;
var credential = new AzureCliCredential();

// Load environment configuration
DotNetEnv.Env.Load("../../../.env");
```

### 🏗️ **ארכיטקטורת זרימת עבודה מותנת**

```mermaid
graph TD
    A[Input: תמצית התוכן] --> B[מבצע הטיוטה
    B --> C[מבצע סקירת התוכן]
    C --> D{החלטת הסקירה}
    
    D -->|כן - אושר| E[מבצע פרסום]
    D -->|לא - נדחה| F[שלח מבצע סקירה]
    
    E --> G[שמור כקובץ Markdown]
    F --> H[הודע על תיקונים]
    
    I[מיקרוסופט פאונדרי] --> B
    I --> C
    I --> E
    
    J[בנג גראונדינג] --> B
```

**רכיבים מרכזיים:**
- **מבצע טיוטות**: סוכן AI שיוצר טיוטות תוכן ראשוניות מהקצאות
- **מבצע סקירת תוכן**: סוכן AI שמעריך את איכות הטיוטה והתאמתה
- **ניתוב מותנה**: לוגיקת החלטה שמנתבת בהתאם לתוצאות הסקירה
- **מסלולי פרסום/סקירה**: מסלולי עיבוד נפרדים לתוכן מאושר מול נדחה
- **ניהול מצב**: שומר הקשר של תוכן והסקירה לאורך זרימת העבודה

## 🎨 **תבניות עיצוב זרימת עבודה מותנות**

### 📋 **ייצור תוכן עם שערי איכות**
```
Outline → Draft Creation → Quality Review → {Approve: Publish | Reject: Revise}
```

### 🎯 **עיבוד מסמכים מבוסס סיכון**
```
Document → Risk Assessment → {Low: Standard | High: Enhanced Review}
```

### 🔍 **ניתוב חכם לשירות לקוחות**
```
Customer Query → Analysis → {Simple: FAQ Bot | Complex: Human Agent}
```

### 💼 **זרימות עבודה מבוססות תאימות**
```
Content → Compliance Check → {Pass: Publish | Fail: Legal Review}
```

## 🏢 **יתרונות מותנים ארגוניים**

### 🎯 **אוטומציה חכמה**
- **החלטות ניתוב חכמות**: החלטות מונעות AI על בסיס ניתוח ומצב התוכן
- **עיבוד אדפטיבי**: זרימות עבודה שמסתנכרנות אוטומטית עם תנאים משתנים
- **אכיפת כללים עסקיים**: יישום אוטומטי של לוגיקה עסקית ומדיניות מורכבת
- **ניתוב מבוסס הקשר**: החלטות על סמך היסטוריית הזרימה והקשר מצטבר

### 📈 **מצוינות תפעולית**
- **הקצאת משאבים אופטימלית**: ניתוב עבודה למומחים ותהליכים המתאימים ביותר
- **הפחתת התערבות ידנית**: החלטות אוטומטיות מצמצמות את הצורך בנתוב ידני
- **קיצור זמני פתרון**: ניתוב ישיר למומחיות ויכולות עיבוד רלוונטיות
- **יישום עקבי**: יישום אחיד של חוקים עסקיים וקריטריוני החלטה

### 🛡️ **ניהול סיכונים ותאימות**
- **הערכת סיכון אוטומטית**: הערכה מונעת AI של תוכן ורמות סיכון
- **אכיפת תאימות**: ניתוב אוטומטי דרך תהליכים רגולטוריים נדרשים
- **יישום פרוטוקולי אבטחה**: אמצעי אבטחה ממוגברים על פי הערכת סיכון
- **ניהול מעקב ביקורת**: תיעוד מלא של החלטות הניתוב והנימוקים

### 📊 **אנליטיקה ושיפור מתמיד**
- **אנליטיקת החלטות**: מעקב אחר יעילות ודיוק החלטות הניתוב
- **זיהוי תבניות**: זיהוי מגמות ותבניות בהחלטות ניתוב לאורך זמן
- **אופטימיזציה של ביצועים**: שיפור מתמיד של קריטריוני החלטה ויעילות ניתוב
- **בינה עסקית**: תובנות על מאפייני תוכן ודרישות עיבוד

### 🔧 **מצוינות טכנית**
- **ניהול מצב מתמיד**: שמירת מצבים מורכבים לאורך ביצוע זרימת העבודה
- **ארכיטקטורה מתקדמת להרחבה**: טיפול בדרישות עיבוד מותנות בהיקף גבוה
- **יכולות שילוב**: אינטגרציה חלקה עם מערכות ותהליכים עסקיים קיימים
- **ניטור ותצפית**: מעקב מקיף אחר ביצועי הזרימות והחלטות

בואו נבנה זרימות עבודה חכמות, מונעות החלטות ברמת הארגון עם .NET! 🚀

## 💻 הרצת הקוד

היישום המלא זמין ב־`04.dotnet-agent-framework-workflow-aifoundry-condition.cs`. זה מדגים **זרימת עבודה לייצור תוכן עם שערי איכות**:

### 🏗️ **ארכיטקטורת זרימת עבודה**

```
Content Outline → Draft Creation → Quality Review → Conditional Routing:
                                                      ├─ Approved (>200 words) → Publish
                                                      └─ Rejected (<200 words) → Review Notification
```

**סוכנים בזרימה:**
1. **סוכן אוונגליסט**: יוצר טיוטות מדריך מהקצאות עם עיגון Bing
2. **סוכן מבקר תוכן**: מעריך איכות הטיוטה (מספר מילים, שלמות)
3. **סוכן מפרסם**: שומר תוכן מאושר כקובצי Markdown עם חותמת זמן

**מבצעים מותאמים אישית:**
1. **DraftExecutor**: מארגן יצירת טיוטות
2. **ContentReviewExecutor**: מבצע הערכת איכות
3. **PublishExecutor**: מנהל פרסום תוכן מאושר
4. **SendReviewExecutor**: מנהל הודעות על תוכן שנדחה

### 🚀 הרצת הדוגמה

**דרישות מוקדמות:**
- סביבת Microsoft Foundry מוגדרת
- אימות CLI של Azure (`az login`)
- (אופציונלי) חיבור Bing Search לעיגון

```bash
# הפוך את הסקריפט לריצה (Unix/Linux/macOS)
chmod +x 04.dotnet-agent-framework-workflow-aifoundry-condition.cs

# הפעל את זרימת העבודה המותנית
./04.dotnet-agent-framework-workflow-aifoundry-condition.cs
```

או על Windows:
```powershell
dotnet run 04.dotnet-agent-framework-workflow-aifoundry-condition.cs
```

### 📝 פלט צפוי

זרימת העבודה תעשה:
1. **יצירת סוכנים**: אתחול שלושה סוכני Microsoft Foundry מתמחים
2. **יצירת טיוטה**: סוכן אוונגליסט יוצר טיוטת מדריך מהקצאה
3. **סקירת תוכן**: סוכן מבקר תוכן מעריך את איכות הטיוטה
4. **ניתוב מותנה**:
   - **אם מאושר (>200 מילים)**: מבצע הפרסום שומר כקובץ Markdown
   - **אם נדחה (<200 מילים)**: שליחת הודעת ביקורת
5. **הצגת תוצאות**: הצגת תוצאת הזרימה הסופית

### 🔧 אפשרויות התאמה אישית

**שינוי קריטריוני סקירה:**
```csharp
const string ContentReviewerInstructions = @"
You are a content reviewer...
1. Check if content is more than 500 words (instead of 200)
2. Verify technical accuracy
3. Ensure proper formatting
...";
```

**הוספת מסלולי ניתוב נוספים:**
```csharp
var workflow = new WorkflowBuilder(draftExecutor)
    .AddEdge(draftExecutor, contentReviewerExecutor)
    .AddEdge(contentReviewerExecutor, publishExecutor, condition: GetCondition("Excellent"))
    .AddEdge(contentReviewerExecutor, editExecutor, condition: GetCondition("Good"))
    .AddEdge(contentReviewerExecutor, sendReviewerExecutor, condition: GetCondition("Poor"))
    .Build();
```

**שינוי דרישות תוכן:**
```csharp
string OUTLINE_Content = @"
# Your Custom Topic
## Section 1
https://your-reference-url
## Section 2
...
";
```

### 🎯 יישומים מעשיים

תבנית זרימת העבודה המותנת אידיאלית ל:
- **מערכות ניהול תוכן**: זרימות אוטומטיות עם שערי איכות
- **עיבוד מסמכים**: ניתוב מסמכים לפי סיווג ותאימות
- **תמיכת לקוחות**: ניתוב חכם של תיקים לפי מורכבות ודחיפות
- **סקירה משפטית**: ניתוב חוזים לפי הערכת סיכון וערך
- **תהליכי משאבי אנוש**: ניתוב בקשות דרך מסלולי סינון מתאימים

### 🔍 הבנת לוגיקה מותנת

**פונקציית תנאי:**
```csharp
public Func<object?, bool> GetCondition(string expectedResult) =>
    reviewResult => reviewResult is ReviewResult review && review.Result == expectedResult;
```

פונקציה זו יוצרת ניבוי ש:
1. בודקת האם התוצאה היא מסוג `ReviewResult`
2. משווה את תכונת `Result` לערך הצפוי
3. מחזירה אמת/שקר לקביעת הניתוב

**קצוות זרימת עבודה עם תנאים:**
```csharp
.AddEdge(contentReviewerExecutor, publishExecutor, condition: GetCondition("Yes"))
.AddEdge(contentReviewerExecutor, sendReviewerExecutor, condition: GetCondition("No"))
```

### 📊 תכונות מתקדמות

**אימות סכימת JSON:**
זרימת העבודה משתמשת בסכימות JSON לוודא תגובות מובנות:

```csharp
// Define response structure
public class ReviewResult
{
    [JsonPropertyName("review_result")]
    public string Result { get; set; } = string.Empty;
    
    [JsonPropertyName("reason")]
    public string Reason { get; set; } = string.Empty;
    
    [JsonPropertyName("draft_content")]
    public string DraftContent { get; set; } = string.Empty;
}

// Apply to agent
ResponseFormat = ChatResponseFormat.ForJsonSchema(
    AIJsonUtilities.CreateJsonSchema(typeof(ReviewResult)), 
    "ReviewResult", 
    "Review Result From DraftContent"
)
```

**אינטגרציית עיגון Bing:**
סוכן האוונגליסט משתמש בעיגון Bing לגישה למידע בזמן אמת:

```csharp
var bingGroundingConfig = new BingGroundingSearchConfiguration(bing_conn_id);
BingGroundingToolDefinition bingGroundingTool = new(
    new BingGroundingSearchToolParameters([bingGroundingConfig])
);
```

זה מאפשר לסוכן לעקוב אחרי כתובות URL במפרט ולחלץ מידע עדכני.

### 🛡️ טיפול בשגיאות

זרימת העבודה כוללת טיפול שגיאות אמין עבור תוכן שנדחה:
- כשלים בסקירה מפעילים מסלול אלטרנטיבי
- הודעות מספקות סיבות ברורות לדחייה
- התוכן נשמר לתיקון מחדש

### 🔄 הרחבת זרימת העבודה

**הוספת לולאת תיקון:**
יצירת לולאת משוב שיוצרת טיוטות מחדש באופן אוטומטי:

```csharp
.AddEdge(contentReviewerExecutor, publishExecutor, condition: GetCondition("Yes"))
.AddEdge(contentReviewerExecutor, draftExecutor, condition: GetCondition("No")) // Loop back
```

**יישום סקירה רב-שלבית:**
הוספת שלבי סקירה מרובים עם קריטריונים שונים:

```csharp
.AddEdge(draftExecutor, technicalReviewer)
.AddEdge(technicalReviewer, editorialReviewer, condition: GetCondition("TechPass"))
.AddEdge(editorialReviewer, publishExecutor, condition: GetCondition("EditPass"))
```

תבנית זרימת העבודה המותנת הזו מספקת בסיס לבניית מערכות אוטומציה חכמות, מתקדמות לארגון! 🚀

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**כתב ויתור**:
מסמך זה תורגם באמצעות שירות תרגום אוטומטי [Co-op Translator](https://github.com/Azure/co-op-translator). למרות שאנו שואפים לדיוק, יש לקחת בחשבון שתרגומים אוטומטיים עלולים להכיל שגיאות או אי-דיוקים. יש להחשיב את המסמך המקורי בשפתו הטבעית כמקור הסמכות. למידע קריטי מומלץ להשתמש בתרגום מקצועי על ידי מתרגם אדם. אנו לא אחראים לכל אי-הבנה או פירוש שגוי הנובע מהשימוש בתרגום זה.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->